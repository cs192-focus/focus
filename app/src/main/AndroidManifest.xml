<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.focus.focusproductivitymanager">

    <application
        android:name=".FocusApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Focus">
        <activity
            android:name=".MainActivity"
            android:label="Focus"
            android:theme="@style/Theme.Focus.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddTaskActivity"
            android:parentActivityName=".TaskListActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".TaskListActivity" />
        </activity>
        <activity
            android:name=".TaskListActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Focus.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".TimerListActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Focus.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".AddTimerActivity"
            android:parentActivityName=".TimerListActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".TimerListActivity" />
        </activity>
    </application>

</manifest>